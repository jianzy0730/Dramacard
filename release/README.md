# Release Artifact

`dramacard-debug.apk` is the debug APK copied from the original local project build at the user's request.

The source tree in this public copy is sanitized separately and does not include raw videos, generated assets, local model weights, or private configuration files.
